package app.entities;

import org.junit.jupiter.api.Test;

import java.math.BigDecimal;

import static org.junit.jupiter.api.Assertions.*;

class QuoteMaterialLineTest {

    // Tests that a quote material line correctly stores stykliste data
//    @Test
//    void testQuoteMaterialLineConstructorAndGetters() {
//        QuoteMaterialLine line = new QuoteMaterialLine(1, 5, 10, 600, new BigDecimal("4.00"), new BigDecimal("149.95"), "Spær monteres på rem");
//
//        assertEquals(1, line.getQuoteLineId());
//        assertEquals(5, line.getQuoteId());
//        assertEquals(10, line.getMaterialId());
//        assertEquals(600, line.getLengthCm());
//        assertEquals(new BigDecimal("4.00"), line.getQuantity());
//        assertEquals(new BigDecimal("149.95"), line.getUnitPrice());
//        assertEquals("Spær monteres på rem", line.getUsageDescription());
//    }
}